---
interface Props {
  class?: string;
  hover?: boolean;
}

const { class: className, hover = true } = Astro.props;
---

<div
  class:list={[
    'glass rounded-2xl p-6',
    hover && 'card-glow gradient-border magic-tilt',
    className,
  ]}
>
  <slot />
</div>
